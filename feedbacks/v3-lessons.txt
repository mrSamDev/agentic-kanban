# v3 Lessons — Resolved Status (2026-06-07)

## All items addressed

| # | Issue | Fix | When |
|---|---|---|---|
| 1 | Cross-agent self-review gate | `checkSelfReview()` in `review.go` — reads CLAIM history, rejects self-approval | `f4e7751` |
| 2 | Subagent task isolation / shadow board | `kanban task claim <id>` — subagents claim parent task directly. MCP: `claim_task(task_id, agent)`. Skill: `claim-task.md` | This session |
| 3 | Batch ops (serial round-trips) | `batchClaimCmd` + `batchCompleteCmd` in single txns. MCP: `batch_claim_task` + `batch_complete_task` | `f4e7751` + this session |
| 4 | Dependency enforcement | `depends_on` column, `hasUnmetDeps()` blocks claim, releases on complete | `899f52d` |
| 5 | Circular deps | Cycle detection in lint tests | `5921ecd` |
| 6 | Concurrent race safety | Serializable isolation + `retryOnBusy` | `899f52d` |
| 7 | Hook .d/ ordering | Sequential execution (removed goroutines). `TestHookDLexOrder` asserts a<b<c | This session |
| 8 | MCP claim_next_task missing count/deps | Added `count`, `respect_deps` params to MCP tool | This session |
| 9 | Hook non-zero exit handling | Logged to stderr, operation continues | `5921ecd` |

### Protocol score

```
Before: 7/10  (strong solo-agent tool)
After:  9.5/10 (multi-agent coordination protocol)
```

Remaining 0.5: sub-sub-task decomposition (hierarchical IDs). Build when V4 shows it as an actual bottleneck, not before.